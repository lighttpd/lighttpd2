if get_option('lua')
  install_data(
    'core__cached_html.lua',
    'core.lua',
    'core__xsendfile.lua',
    'secdownload.lua',
    'secdownload__secdownload.lua',
    install_dir: lua_dir,
    install_tag: 'runtime',
  )
endif
